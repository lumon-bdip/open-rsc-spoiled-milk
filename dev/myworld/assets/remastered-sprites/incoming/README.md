# Incoming Remastered Art

Place untouched deliveries under `<contributor>/<batch>/`, run the documented
classification command, and move reviewed files into named sets. Nothing under
`incoming/` is a runtime or packaging input.
